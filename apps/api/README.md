# Private Gmail Outreach API

The FastAPI service and PostgreSQL-backed worker for the Private Gmail Outreach Engine.

Run the development commands from this directory after copying the root `.env.example` to the repository root.
